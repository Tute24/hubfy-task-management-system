services:
  db:
    image: mysql
    restart: always
    ports:
      - "3306:3306"
    volumes:
      - db_volume:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: passoword
      MYSQL_DATABASE: hubfy-tms-db
      MYSQL_USER: user
      MYSQL_PASSWORD: password

volumes:
  db_volume:
